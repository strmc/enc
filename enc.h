#include <iostream>
#include <vector>
#include <array>
#include <string>
#include <cstring>
#include <fstream>
#include <unistd.h>

int main(int, char**);

void encrypt(char*, const char*);

void decrypt(char*, const char*);

void help(const char*);
